---
name: web-implementation-tips
description: website 実装時の具体的な注意点。独立judgeの採点結果から自動蓄積される。Astro + DaisyUI で多ページサイトを実装する前に読む。
---

# website 実装のヒント

実装段（`scripts/web-implement-minimax.sh`）が読む。
下半分の「自動更新ログ」は `scripts/web-update-skill-from-score.py` が
独立judgeの採点結果から機械的に追記する。**手で編集しない。**

## 実装前チェックリスト（静的節・手で保守する）

- [ ] ページを書く前に `_site.ts` を書いたか（ナビ・フッター・組織情報の単一情報源）
- [ ] 全ページが `SiteLayout` を使っているか（ヘッダー/フッター/パンくず/JSON-LD/title は自動）
- [ ] `description` はページ固有で40文字以上か（`consist-duplicate-description`）
- [ ] 各ページで `src/components/` を2件以上、サイト全体で6種類以上使ったか
- [ ] 独自テーマを `src/styles/themes/` に起こし、`global.css` の列挙にも足したか
      （列挙し忘れると静かに既定へフォールバックする）
- [ ] 文字色と背景のコントラストは足りているか（**実測で比 1.00 の白地に白文字を出した事故がある**）
- [ ] `loading="lazy"` / `width` / `height` / `alt` を img に付けたか
- [ ] ページ内 `<style>` でアニメーションを書いたなら
      `@media (prefers-reduced-motion: reduce)` の打ち消しを付けたか
- [ ] `<style is:global>` で DaisyUI 共通クラス（`.btn` `.card` 等）を上書きしていないか
      （1ビルドに複数サイトが載るので他サイトを壊す）
- [ ] 「準備中」「coming soon」等の未完成文言を残していないか（**high で検出される**）
- [ ] ディレクションの `data-role="animation"` を全部実装したか。
      import しただけで使っていないコンポーネントは無いか

## 完成条件

```
npm run check:web      # 静的37ルール。high ゼロ
npm run check:render   # 描画検証。high ゼロ
npm run check:catalog  # カタログ掲載漏れ
```

## 自動更新ログ（design-score後に追記。手動編集しない）

<!-- scripts/web-update-skill-from-score.py が run_dir の design-score-data.json から
     issues/strengths をそのまま追記する。意味の解釈はしない（それは読み手の仕事）。
     重複っぽい項目は正規化して機械的に除外する。直近30ラン分だけ保持する。 -->

### corporate-20260903-1448 (corporate, 2026-09-09)

**a** (total=59)
- ⚠ トップの5領域案内に続き、お知らせ・採用・問い合わせの再掲、関連ページ、CTA、フッターが重なる。同じ遷移先の反復が多く、スクロール量に対して得られる情報が少ない。
- ⚠ トップの各領域が同じ大きさの縦長写真と罫線で均等に並び、参照にある写真サイズ・配置・余白の変化を単調な一覧へ置き換えている。
- ⚠ SITE INDEX、PHILOSOPHY、HISTORY、TRAINING PROTEINなど、意味が日本語見出しと重なる装飾的な英語小見出しを連発している。
- ⚠ トップの実績は400処方・2拠点だが、下層には480処方・直営3拠点とある。撮影時点では整合せず、集計時点や数値の関係も説明されていない。
- ⚠ 会社ページ末尾の『会社について、もっと詳しく。』に対するボタンが『製品ラインを見る』で、予告内容と遷移先が一致していない。
- ✓ 製品ラインという具体的なナビ名を使い、下層にはパンくずと現在地表示がある。
- ✓ 会社ページには理念・代表・沿革・会社概要、製品ページにはカテゴリ説明・製造工程・店舗情報があり、下層の内容量は確保されている。
- ✓ トップの『分類名・写真・下層リンク』という構成には、参照サイトからの構造的な学習が認められる。

**b** (total=61)
- ⚠ トップの主コピーが『動く体／に、確か／な補給／を。』と意味の途中で分断されている。文字の大きさと列幅の組み合わせが、読みやすさを損ねている。
- ⚠ ヒーローの4枚組写真は、走行・休息・製造・素材を示そうとしているが、右下の紙片のような接写が何を伝えるのか分かりにくい。画像間の関係を説明する情報もない。
- ⚠ トップの5領域案内から写真を取り除いた結果、短いリンク群に対して大きな空白が続いている。参照サイトの中心だった写真と余白の組み合わせが失われている。
- ⚠ 会社・製品ページの濃紺タイトル帯と明朝体は統一されているものの、参照の軽快なサンセリフ、大胆な写真、非均等な配置とのつながりが弱い。
- ⚠ PHILOSOPHY、HISTORYなどの小見出しに加え、Philosophy Statement、Product Linesという大きな英語も重ねており、日本語タイトルと重複する装飾が多い。
- ✓ 紺・灰緑・白の配色と明朝見出しがページ間で揃い、aより視覚的な統一感がある。
- ✓ 沿革を左揃えの年表として整理し、aの小さな枠が密集した表示より追いやすくしている。
- ✓ 下層の情報量を維持し、製品カテゴリの番号や店舗の営業時間など、比較・確認に役立つ要素を加えている。
