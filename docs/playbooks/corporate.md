# プレイブック: コーポレートサイト

Look 段の解析結果を蓄積したサイト種別別ドキュメント。
Direction 段と実装 SKILL の入力として参照する。
観測ログと機械集計は `scripts/web-update-playbook.py` が自動更新する。

**これは一般論のガイドではなく、実測の積み上げ**である。1件しか観測して
いないことは「要追加観測」と明記し、断定しない。

## 機械集計（自動更新。手動編集しない）

### 参照サイトの配色（頻度）

`#000000`×3 `#FFFFFF`×2 `#F5F5F5`×2 `#3A689A`×1 `#E7E5D9`×1 `#D8E6E7`×1 `#F4F4F2`×1 `#202020`×1 `#FF00FE`×1 `#FF00FF`×1 `#FF5700`×1 `#D8FF1B`×1

### モーション実測（同種別の全ランを集計）

- リビール種別: fade 147 / fade-up 45 / other 25 / fade-side 3
- duration(median) の観測: [700, 700, 900, 900] ms
- easing: `ease`×7 / `cubic-bezier(0.165, 0.84, 0.44, 1)`×2
- ヘッダーのスクロール挙動: 変化なし×1 / shrinks,hides,togglesClass×1 / hides×1
- prefers-reduced-motion 尊重: あり 0 / なし 4（参照が非対応でも真似しないこと）


## 観測ログ

### run: corporate-20260909-1021（2026-09-09）

参照: MontBlanc Pictures （https://mtblanc.jp/） / 業種: 制作・開発・企画・マーケティング・コンサル

### 1. サイトマップ

解析対象はトップ・Works一覧・CG Direction絞り込みの3画面。以下の寸法は幅1440pxの元キャプチャ基準。高さは概算、色はJPEG画素の実測値。未収録ページの内部構成とモバイル操作は未確認。

グローバルナビは次の6項目。括弧内はページの役割。

- **Works** — 受託制作の実績を一覧・カテゴリから探す。
  - **All** — 実績全体を表示する。
  - **CG Direction** — CGディレクション実績を抽出する。添付3枚目に該当。
  - **Film Directing**
  - **Character Design**
  - **Digital Signage**
  - **Exhibition Contents**
  - **Motion Capture**
  - 以下、フッターにも露出する実績分類：**Installation / Original / Projection Mapping / VR / Interactive / Game / StationId / Animation / Character Animation / CM / PV / Live Action / Motion Graphics / Opening / VFX / Show Reel / Montblanc Made™(Original)**
  - 個別実績 — 画像・案件名から進む詳細導線。詳細画面は未収録。
- **Montblanc Made™** — 自社オリジナル作品を受託実績と分けて紹介する。
- **Journal** — 記事を通じて活動・社内情報・実績の更新を伝える。
  - フッター表記：**Event / Media / Report / Award / Dialy / Info / Press / Staff Interview**
  - `Dialy` は画面上の綴りに従う。
- **Recruit** — 採用情報への入口。
- **About us** — 会社・スタッフ・制作環境を知る入口。
- **Contact** — 制作相談・問い合わせの受け皿。

ナビ外の導線：

- **トップ** — ロゴから戻る起点。主要6領域の抜粋を並べる。
- フッターの **Works** 配下に個別案件名を4件露出。
- フッターの **Montblanc Made™** 配下に作品名を9件露出。
- Instagram・Facebook・X・Spotifyへの外部導線を4件配置。
- 独立した法務・プライバシーページへのリンクは、添付範囲では確認できない。

分類ラベルは確認できるが、各リンクが独立ページ・絞り込み・外部ページのどれに当たるかは、CG Direction以外は未確定。

### 2. ページ共通のシェル

| 要素 | 観測した構造 |
|---|---|
| ヘッダー／ロゴ | 全幅の横長ヘッダーではなく、上端中央のロゴと、本文に重なる横並びナビを分離。下層のロゴは幅約220px、高さ約135px、上端約25px。 |
| グローバルナビ | 6項目を白いピル形状で横一列に配置。高さ約48〜52px。本文・フッター上に重なるため固定配置が示唆されるが、画像だけでは固定基準を断定できない。 |
| CTA | `Contact` が共通導線。他項目と同系統の見た目で、問い合わせだけを大きく強調していない。 |
| 補助導線 | 画面右端に丸いアイコンを縦積み。各アイコンの機能は画像のみでは確定できない。 |
| モバイル | モバイル幅の収録なし。メニューの開閉方式、ブレークポイントとも未確認。 |
| パンくず | 収録した下層2画面に可視のパンくずはない。`Category` 行は実績分類であり、パンくずではない。 |
| 下層ページヘッダ帯 | 白単色、全幅、高さ約400px。中央上部にロゴ、下端に巨大な `Our Works`。下隅に約24〜32pxの角丸。2画面で共通。 |

**フッターは2段構成。**

1. 上段：左右約1：1の2列。左にロゴ・会社情報・SNS、右に6項目のサイトマップ。
2. 下段：中央に大きな黒い山形のブランド図形。下端まで展開する。

右列では **Works / Montblanc Made™ / Journal** の下に分類・作品名を小文字級で展開し、**Recruit / About us / Contact** は大見出しとして並べる。大項目だけで6件、さらに実績分類約26件、個別案件4件、オリジナル作品9件、記事分類8件を露出する。

会社情報は「モンブラン・ピクチャーズ株式会社」、郵便番号、番地までの住所、SNS4件。電話・営業時間は見当たらない。フッター全体は高さ約1500px、約1.7画面分あり、情報一覧とブランド図形に大きく面積を配分する。

### 3. トップページの構成

全長13337px、実測14.8画面分。1画面を約900pxとして換算する。

| 順序・セクション | 役割 | 主要素 | 高さ目安 |
|---|---|---|---|
| 1. ヒーロー | 制作する映像・キャラクターの領域を最初に見せる | 全幅の作品ビジュアル、上端中央ロゴ、中央の輪郭文字 `STAY / OUT / IMAGINATION`、下部に複数の作品断片、重なるナビ | 約1120px／1.25画面 |
| 2. Pickup Works | 代表実績からWorksへ送る | 左に見出し、右に `View More` と丸矢印。濃い青灰背景に作品カードを約3列×3段。画像・案件名・分類を重ね、横方向の棚状構造で区切る | 約2080px／2.3画面 |
| 3. Fresh Journal | 新着活動・記事を紹介する | 白背景。先頭の大きい記事と小さい記事群を組み合わせた非均等グリッド。約9件。画像、記事名、小さな分類・日付情報 | 約1680px／1.9画面 |
| 4. Recruit | 採用を独立した閲覧目的として提示する | 黄背景。巨大な「求人募集」「新卒／キャリア」等の文字、人物・集合写真・記事カード約8点。右上に一覧導線 | 約1520px／1.7画面 |
| 5. About us | 人と制作環境から会社を理解させる | 左右約1：1。左は緑背景に白い有機形状と人物の丸い切り抜き約5点。右はオフィス内外・制作環境の写真コラージュ約8点 | 約1900px／2.1画面 |
| 6. Montblanc Made™ | 自社作品群を受託実績から区別する | 青灰背景、見出しと一覧導線、横長の棚2段。パッケージ・キャラクター等が見える作品ビジュアル6点以上。左右に途中で切れる要素あり | 約1340px／1.5画面 |
| 7. 共通フッター | 全領域への再分岐と会社情報の提示 | 会社情報、詳細サイトマップ、巨大な山形図形 | 約1520px／1.7画面 |

セクション間には灰色の空白が入り、ヒーロー後は約200px、以降は概ね380〜420px。表の高さにこの空白は含めていない。

トップは長文で会社を説明する構成ではなく、**実績→活動→採用→会社→自社作品**を画像中心で紹介する分岐ページ。About us右列には大きな白い空白が見えるが、意図した余白か、動的要素・読み込みに由来するものかは確定できない。

### 4. 下層ページの構成

**Works一覧：全長4008px、実測4.5画面分。**

| 順序 | 役割・主要素 | 高さ目安 |
|---|---|---|
| ページヘッダ | 中央ロゴと `Our Works` で実績領域を明示 | 約400px |
| 分類バー | 黒地の `Category`、白いピル形状の `All` とカテゴリ群。折り返さず右端で続きが切れる | 約50px |
| 実績グリッド | 3列×7段＝21件。各カード約480×270px、約16：9。画像上の下部中央に白い案件名、その下に小さい分類ピル | 約1890px |
| ページ送り | 全幅黒背景。中央 `Page 1 / 10 ∨`、右端 `Next` と白い丸矢印 | 約140px |
| 共通フッター | トップと同じ会社情報・サイトマップ・図形 | 約1520px |

カード間の余白はほぼ0〜数px。個々の角丸によって境界を作る。説明文を別枠にせず画像内に収め、一覧で比較できる件数を優先する。

**CG Direction絞り込み：全長2242px、実測2.5画面分。**

| 順序 | 役割・主要素 | 高さ目安 |
|---|---|---|
| ページヘッダ | 一覧と同じ `Our Works`。カテゴリ名への見出し変更なし | 約400px |
| 分類バー | 一覧と同じカテゴリ列 | 約50px |
| 絞り込み結果 | `TEM – The Pulse of Light` 1件。カード幅は約480pxのまま左寄せし、残り2列分は灰色の空白 | 約270px |
| 共通フッター | 結果カード直後から配置。ページ送りはない | 約1520px |

**トップとの作り込みの差：**下層はセクション数・レイアウト変化・モーション量が明確に少ない。一方、一覧は21件の実績と分類・ページ送りを備えており、内容が空疎なページではない。絞り込み画面は同一テンプレートの結果1件の状態で、独立したカテゴリ説明はない。

共通性は「ロゴ・巨大な黒文字・ピル・画像上の案件名・共通フッター」で維持される。ただし絞り込み後も見出しが同じで、画像では選択中カテゴリの強調も弱く、現在の条件を把握しにくい。

### 5. ビジュアルシステム

**配色：**以下は元JPEG内で頻出する完全一致画素。CSS指定色そのものを確認した値ではない。

| 系統 | 実測HEX | 用途 |
|---|---|---|
| メイン | `#000000` | 見出し、ロゴ、ページ送り、フッター図形 |
| アクセント | `#F8D473` | Recruit背景 |
| アクセント | `#5EBEA2` | About us左面 |
| アクセント | `#82A5B8` | Montblanc Made™背景 |
| 背景 | `#FFFFFF` | 下層ヘッダ、Journal、フッター、ピル |
| 背景 | `#DFDBDA` | ページ地、セクション間、検索結果の空き |
| 背景 | `#393F4D` | Pickup Worksの棚全体 |

単一アクセントで全ページを塗る方式ではなく、**共通シェルは白黒、トップの領域識別には面の色**を使う。下層は作品画像の色を主に見せる。

- **タイポ：**欧文見出しは極太の幾何学的なサンセリフ。一部に斜めの字形がある。日本語は太めのゴシック。ロゴのみ筆記体。フォント名は未確認。
- **文字サイズ：**`Our Works` は幅約740pxを占め、文字サイズ換算で約140〜160px。トップの通常セクション見出しは約75〜90px、フッター大項目は約60〜70px。求人の背景文字は200px級以上。
- **和欧混植：**ナビ・領域見出しが英語、案件名・採用情報・会社情報が日本語。本文段落より短い見出しとラベルが多い。
- **画像内容：**中心は完成したCG・映像・展示の実績。人物の寄り、キャラクターの寄り、展示空間の引きを混在させる。会社・採用部分では社員とオフィスの写真を使用。
- **点数：**Works一覧21点、絞り込み1点。トップはヒーローを除いて約45点以上の画像・切り抜きが見える。重複や部分表示があるため、ユニーク素材数ではない。
- **余白：**セクション内部は高密度、セクション間は約400px。下層の分類→カード→ページ送り→フッターはほぼ連続する。トップと下層で余白の役割を変えている。

### 6. モーション

| 項目 | 実測から言えること／実装への落とし込み |
|---|---|
| 支配的リビール | トップ92件中、fade 55件、fade-up 10件、fade-side 2件、other 25件。fadeが約60%。opacity主体を基本とする。 |
| duration | 検出範囲は最小・中央値・最大とも700ms。基本トークンを `700ms` に置ける。 |
| easing | `ease` と `cubic-bezier(0.165, 0.84, 0.44, 1)`。どの要素にどちらが対応するかは未確定。 |
| 発火方式 | GSAP 3.0.5、Lenis、Swiperを検出。ただし、個々のリビールがどのライブラリ・classトグルで発火するかは提示データでは特定できない。 |
| スクロール連動 | 4件を検出。ScrollTriggerのtotal・pinned・scrubbedはnullのため、ピン留めやスクラブを使用しているとは断定しない。 |
| ページ差 | トップ92件、Works 0件、CG Direction 0件。検出差92件、`motion_consistent: false`。下層の0件は、全アニメーション不在を意味しない。 |
| ページ遷移 | 遷移ライブラリ・ローダー候補は検出なし。専用のページ遷移演出は確認できない。 |
| reduced-motion | 通常92件→低減設定82件。削減は10件、約10.9%。測定判定は非対応。 |

**ヘッダー計測の注意：**対象は `home-works__header`。高さ42px、`position: static`、topが1397px→−403pxへ移動している。これは作品セクション見出しがスクロールで画面外へ出た結果と解釈でき、グローバルヘッダーの自動非表示を示す証拠にはならない。縮小・背景変更・class切替も検出されていない。

後工程への実装指示としては、基本を700msのfadeとし、移動を伴うリビールは補助に限定する。発火閾値・移動距離・繰り返し・スタガー間隔は未測定なので別途定義する。低減設定ではスクロール補間・自動移動・位置変化を停止し、コンテンツを初期表示する。これは参照の再現ではなく改善仕様となる。

### 7. 借りるべき点 / 借りてはいけない点

**`must_borrow`**

1. **トップを主要領域の入口にする。** 各セクションに見出し・代表コンテンツ・一覧導線を置き、詳細は下層へ分担する。
2. **受託実績と自社作品を分ける。** 依頼可能な制作領域と、自社の創作活動を別のナビ項目として扱う。
3. **実績一覧と分類結果で同じテンプレートを使う。** カード比率・列幅・分類バーを維持し、抽出対象だけを変える。
4. **共通シェルとコンテンツ表現を分ける。** ナビ・ロゴ・フッターの規則を固定し、トップの各領域には別の背景色と画像配置を許す。
5. **フッターで分類階層まで再提示する。** ページ末尾から実績分野・記事分野へ直接移動できるようにする。

**`do_not_copy`**

- MontBlancの名称・ロゴ・山形図形、固有のコピー、作品画像、キャラクター、社員写真。
- reduced-motion設定でも82件のリビールが残る挙動。
- パンくずが見えず、絞り込み後もタイトルが変わらない現在地表示。
- 画像と重なる小さい白文字や、判読しにくい分類ピル。
- 少件数の結果に対して、約1500pxのフッターがページの約68%を占める比率。
- 用途を確認せずに再現する約400pxのセクション間余白や、About us内の大きな白い空白。
- トップ92件・下層0件という演出配分を、そのままページ間の品質基準にすること。

### 8. この業種の website に共通しそうな型

この1件から仮説として抽出できるのは、**完成作品を能力の証拠とし、実績分類から相談へ進ませる企業サイト**という型。

- トップで代表作を見せ、下層では件数と分類によって制作能力を確認させる。
- 制作技法と利用媒体の両方で実績を探せるようにする。この参照ではCG DirectionとDigital Signage等が同じ分類列に混在する。分類軸の分離が必要かは要追加観測。
- 受託実績・自社作品・活動記事・会社情報・採用・問い合わせを別領域として持つ。
- 会社説明は文章だけでなく、社員と制作現場の写真でも補完する。
- 発注検討者と採用候補者という異なる訪問目的を、トップから並列に振り分ける。

巨大な欧文、作品棚、常時重なるナビ、約15画面のトップは、この会社固有の表現である可能性が高い。広告・映像制作会社全体の共通型とするには要追加観測。
hook: Stop
hook: Stop Completed

---

### run: corporate-20260909-0956（2026-09-09）

参照: 焼津市の矯正専門歯科【やいづ原崎矯正歯科】 （https://yaizu-ortho.com/） / 業種: 医療・病院

### 1. サイトマップ

解析対象はトップ・「当院について」・「はじめての方へ」の3ページ。画像は幅1,440px。以下の寸法は、明記した実測値以外はキャプチャからの概算。高さの比較では **1画面 ≒ 900px** とする。

グローバルナビの日本語ラベルと階層は次のとおり。「診療案内」は子ページを束ねる開閉ボタンで、「お知らせ」はトップ内アンカー。独立ページ数とナビ項目数を混同しない。

- **トップページ** — `/`。医院の概要を提示し、医院紹介・初診案内・診療別ページへ振り分ける。デスクトップではロゴから、モバイルではメニュー項目からも到達する。
- **当院について** — `/about/`。診療方針の背景、院長、院内空間、設備を説明する。
- **はじめての方へ** — `/new-patients/`。初診から治療までの手順と来院方法を説明する。
- **診療案内** — ナビの親項目。
  - **大人の矯正** — `/adult_orthodontics/`。成人向け矯正の案内先。
  - **子どもの矯正** — `/child_orthodontics/`。小児向け矯正の案内先。
  - **ホワイトニング** — `/whitening/`。ホワイトニングの案内先。
- **お知らせ** — `/#news`。診療日の更新などを確認する。

後半3つの診療ページはリンクの存在を確認した範囲で、本文構成は今回の解析対象外。ナビの表記・リンク構造は[参照サイト](https://yaizu-ortho.com/)で確認。

フッターのサイトマップは同じ階層を反復する。**フッターだけに追加された独立した内部ページは確認できない**。別枠で次の機能を露出する。

| 表記 | 役割 |
|---|---|
| 診療時間 | 診療時間情報への導線 |
| アクセス | 来院情報への導線 |
| 診療カレンダー | 月別診療日の確認 |
| 24時間Web予約 | 外部予約サービスへの導線 |
| LINE相談 | 外部相談窓口への導線 |
| Instagram | 外部SNSへの導線 |

トップ本文の「院長紹介」「院内・設備紹介」は、医院紹介ページ内の詳細へ進む導線。「ワイヤー矯正」「マウスピース型矯正装置」はトップの診療カード上の分類であり、グローバルナビの階層とは別に扱う。

### 2. ページ共通のシェル

**ヘッダー**

- 高さは実測 **81px、スクロール後80px**。`position: fixed`。
- 左端に医院ロゴと地域・診療科の補助表記。右側にナビ、電話番号、白いピル型の「24時間Web予約」。
- 直線的な帯ではなく、ロゴ側の白地と右側の青地を曲線で接続する。
- デスクトップの主ナビは4親項目。医院名をクリックするとトップへ戻る。
- モバイル用に**2本線のメニューボタン**と開閉対象のナビDOMがある。モバイルではトップ項目と英語補助ラベルも加わる。展開方向・画面占有率・開閉時間は今回未実測。
- 下方向へのスクロールで画面外に隠れる挙動を検出。詳細は第6節。

**フッター**

全ページで次の**4段構成**を反復する。

| 段 | 内容と配置 |
|---|---|
| 1. 相談・予約帯 | 青背景。左に相談を促す文、右に電話とWeb予約・LINE相談。下端を波形にする |
| 2. 医院基本情報 | 左に医院名、診療領域、住所、交通、診療時間、駐車場・個室情報、決済案内。右に地図枠 |
| 3. 診療カレンダー | 見出しと埋め込み領域。キャプチャでは大きな空白として見える |
| 4. 最下段 | 左に外観写真、右にサイトマップ・SNS・ロゴ。淡いベージュの面でまとめる |

- サイトマップは**親5項目＋子3項目＝8ラベル**。うち「診療案内」は分類名なので、リンク先は7件。
- 医院情報は住所だけでなく、平日／土日祝の時間区分、月ごとに変わる診療日、来院手段、支払い方法まで載せる。
- キャプチャ上の共通下部領域は約 **2.8〜3.0画面**。地図・カレンダーの空白も含むため、そのまま適正寸法とみなさない。
- 地図とカレンダーは外部埋め込みの存在を確認できるが、画像では内容が表示されていない。恒常的な表示不良か、撮影時の取得条件によるものかは未確定。[共通情報の確認元](https://yaizu-ortho.com/)

**パンくず**

- 添付の下層2ページでは、`トップページ > 現在地` のような**可視パンくずは確認できない**。
- ページ名の下にある英語は補助表記であり、パンくずではない。
- 借用時にはページヘッダの上または下にパンくずを追加する。

**下層ページのページヘッダ帯**

- 下層2ページで共通。ページ先頭から約 **500〜560px**、うち上部約80pxは固定ヘッダーと重なる。
- 左約38〜40％に日本語ページ名と小さな英語、右約60〜62％に医院写真。
- 背景は本文と同じオフホワイト。右写真を上端・右端まで寄せ、左側から細い青線の曲線を重ねる。
- 写真だけを差し替え、タイトル位置・幅比率・装飾の規則を維持する。

### 3. トップページの構成

全長は **10,933px／実測12.1画面**。概要を見せて下層へ送る構成で、最後に全ページ共通の来院・予約情報へ接続する。

| 順序・セクション | 役割 | 主要素・配置 | 高さ目安 |
|---|---|---|---|
| 1. メインビジュアル | 診療への心理的な入口をつくる | 左上に短い日本語コピー、右上に小さな補助文。下に横長の受付写真1枚。細い曲線を写真と余白にまたがせる | 約1.2画面 |
| 2. お知らせ | 更新情報を短時間で確認させる | 左に縦方向のラベル、右に日付付き3行。細い罫線と右端の開閉記号 | 約0.5画面 |
| 3. 導入メッセージ | 初診案内へ送る | 左にスタッフの縦長写真、右に見出し・短段落・小型リンク。右端に補助写真と大きな淡色欧文 | 約1.0画面 |
| 4. 医院の特徴 | 選ぶ理由を3点提示する | 全幅青背景。写真3枚、説明3組、番号01〜03。画像の左右と上下位置をずらす。医院紹介へのリンク | 約2.3画面 |
| 5. 診療案内 | 治療内容別に入口を分ける | 上に診療室の全幅写真。下は左に見出し・説明、右に2列×2段の写真付きカード。カード群を写真側へ重ねる | 約1.5画面 |
| 6. 院長紹介 | 治療担当者の背景を提示する | 左に挨拶文と経歴、右に横長の院長写真と詳細リンク | 約1.0画面 |
| 7. 院内紹介 | 来院前に空間を理解させる | 見出しの下に写真4枚を横並び。各写真に縦書きラベル。背面に大きな淡色欧文と詳細リンク | 約0.8画面 |
| 8. 共通フッター | 予約と来院準備を完結させる | 相談帯→医院情報・地図→カレンダー→外観・サイトマップ | 約2.8画面 |

本文からの導線は、**対話への不安→初診案内、医院への信頼→医院紹介、治療への関心→診療ページ**という対応になっている。全セクションを予約ボタンだけで終わらせず、関心に対応する下層ページを置く。

### 4. 下層ページの構成

**「当院について」**

全長 **13,705px／実測15.2画面**。トップより約26％長い。

| 順序・セクション | 役割 | 主要素・配置 | 高さ目安 |
|---|---|---|---|
| 1. ページヘッダ | 現在のページを示す | 左にページ名、右に入口の写真 | 約0.6画面 |
| 2. 医院の考え方 | 方針を接客場面と結びつける | 写真と文章の2列。その下に大きさの異なる写真2枚を離して配置し、曲線で接続 | 約1.8画面 |
| 3. 院長紹介 | 専門性・経歴を詳しく示す | 左に挨拶、右に人物写真。下に経歴・学会・研修実績の3列 | 約1.4画面 |
| 4. 医院の特徴3点 | トップで示した選定理由を再提示する | 青背景、写真3枚、番号01〜03。トップと同じ構図を反復 | 約2.3画面 |
| 5. 院内紹介 | 部屋ごとの用途を説明する | 受付・待合、相談・診療、サポートの3群。各群に主写真1枚＋補助写真2枚。文章との左右配置を交替 | 約4.0画面 |
| 6. 設備紹介 | 診療を支える機器を説明する | ベージュ背景。写真・名称・説明の3列。上辺に波形 | 約1.7画面 |
| 7. 関連ページ | 次の検討先へ送る | 「はじめての方へ」「大人の矯正」の横長カード2枚 | 約0.4画面 |
| 8. 共通フッター | 予約・来院情報を提示する | トップと同じ4段 | 約3.0画面 |

**作り込みはトップと同等以上。** 写真を増やすだけでなく、経歴の分類、空間の用途別説明、機器別説明まで展開している。ただし特徴3点はトップとほぼ同内容で、詳細化による追加情報は少ない。[本文構造の確認元](https://yaizu-ortho.com/about/)

**「はじめての方へ」**

全長 **9,282px／実測10.3画面**。トップより約15％短い。

| 順序・セクション | 役割 | 主要素・配置 | 高さ目安 |
|---|---|---|---|
| 1. ページヘッダ | 初診案内であることを示す | 左にページ名、右に受付写真 | 約0.6画面 |
| 2. 診療方針 | 説明・治療判断の方針を示す | 左に大きな人物写真。右に見出しと白いカード3枚。上段1枚、下段2枚 | 約1.4画面 |
| 3. 治療の流れ | 相談から治療後までの見通しを示す | 共通工程01〜04の後、成人と小児の2列に分岐。成人は05〜07、小児は05〜10。段階番号、罫線、補足枠を使用 | 約3.1画面 |
| 4. 地図領域 | 場所の把握を補助する | 全幅の地図埋め込み領域と外部地図リンク。画像では空白 | 約0.5画面 |
| 5. アクセス | 来院方法を具体化する | 医院情報と連絡・支払い情報の2列。その下に交通手段別の案内を2列×2段 | 約1.3画面 |
| 6. 関連ページ | 年齢別の診療説明へ送る | 「大人の矯正」「子どもの矯正」のカード2枚 | 約0.4画面 |
| 7. 共通フッター | 予約・来院情報を再提示する | 同じ4段。直前のアクセス情報と一部重複する | 約3.0画面 |

**写真演出は少ないが、下層が簡素とはいえない。** 固有の工程図と対象者別の分岐があり、閲覧目的に合わせて情報表現を変えている。工程は共通4項目＋成人3項目＋小児6項目の**計13項目**。[本文構造の確認元](https://yaizu-ortho.com/new-patients/)

### 5. ビジュアルシステム

**配色**

以下は添付JPEGの画素集計による実測値。CSS指定値ではない。

| 系統 | 実測色 | 使用箇所 |
|---|---|---|
| メイン | **`#3A689A`** | 特徴セクション、予約帯、ヘッダー右側、小型ボタン |
| アクセント | **`#E7E5D9`** | 設備紹介背景、最下段フッター、淡い装飾 |
| アクセント補助 | **`#D8E6E7`** | 初診フローの冒頭をまとめる面 |
| 基本背景 | **`#F4F4F2`** | 3ページの本文背景 |
| 白背景 | **`#FFFFFF`** | カード、予約ボタン、フッターの情報領域 |

トップ・医院紹介とも、青のベタ面が画像全体の約13〜16％を占める。青を小部品だけでなく、長いページの区切りとして使う。

**タイポグラフィ**

- 日本語はゴシック系。読み込まれている日本語フォントは **Zen Kaku Gothic New、400／500**。
- 欧文には **Outfit、500** の読み込みがある。見出し・数字・装飾文字で和欧混植。
- 実読用の大見出しは幅1,440px時で概ね **32〜44px**、本文は **14〜16px程度**。サイズは画像からの推定。
- 欧文セクション名を大きく、その下に日本語を小さく置く例が多い。日本語の主要コピーは2〜3行に分け、字間と行間を広く取る。
- 淡色の背景欧文・番号は本文階層から外し、**80〜160px相当**まで拡大する。
- 借用時は日本語だけでも見出し階層とリンク先が判別できるようにする。

**写真**

キャプチャ内の表示枠を数えた概算。ロゴ・装飾・決済バナーは除き、再掲写真は別枠として数える。

| ページ | 写真枠数 | 主な被写体 |
|---|---:|---|
| トップ | 約17枠 | 受付、スタッフ、院長、診療室、治療関連、院内小物、外観 |
| 当院について | 約23枠 | 対話、院長、受付・待合・診療・サポート空間、機器、外観 |
| はじめての方へ | 約5枠 | 受付、スタッフ、関連ページの人物写真、外観 |

- 空間写真は引き、人物は上半身中心、治療・機器は寄り。被写体に応じて撮影距離を分ける。
- 主写真と補助写真の面積差は概ね **2〜4倍**。全写真を同じカード寸法にそろえない。
- 写真角は小さく丸める。人物・室内・機器を混在させ、同じ用途の写真だけを連続させない。

**余白とレイアウト**

- 本文の左右余白は概ね **画面幅の8〜12％**。写真帯は左右3％前後、または全幅まで広げる。
- 主セクションの上下余白は概ね **120〜220px**。段落・カード内は **16〜32px**。
- 非対称な写真構成と、情報を比較させる等幅グリッドを使い分ける。
- 地図・カレンダー部分の空白は通常の余白設計と分けて扱う。未表示状態を含むため、再現対象にはしない。

### 6. モーション

**支配的なリビール**

- 集計上は **fade-up 9件、fade-side 1件**。
- durationの検出値は最小・中央値・最大とも **900ms**。
- easingは **`ease`**。
- 実装への翻訳例は、opacityとY方向のtransformを900msで補間する方式。ただし移動距離、delay、発火位置、再発火条件は実測データにないため未確定。
- fade-sideは1件のみ。左右どちらから入るかは追加観測が必要。

**発火の仕組み**

- アニメーションライブラリは検出なし。ただし、ライブラリ不使用を完全に証明するデータではない。
- ヘッダーでは `is-shown`・`is-mv` のclass変化を確認。
- 本文リビールの監視方法やclass名は未確定。移植するなら **IntersectionObserverで表示用classを付与する設計**にできるが、これは実装提案であって参照コードの断定ではない。
- スクロール連動要素は **1件**。pin・scrub・トリガー総数は`null`であり、0件として扱わない。

**ヘッダーのスクロール挙動**

| 状態 | 実測 |
|---|---|
| ページ上端 | 高さ81px、`top: 0`、`is-shown is-mv`あり |
| スクロール後 | 高さ80px、`top: -80px`、上記classなし |
| 背景 | 両状態とも要素の背景値は透明。背景色切り替えは未検出 |

主要動作は**ヘッダーを高さ1個分だけ上へ退避すること**。`shrinks: true`でも高さの差は1pxなので、縮小演出を中心に解釈しない。上方向スクロール時の再表示条件は未実測。

**ページ間の演出量**

| ページ | リビール検出数 | duration |
|---|---:|---|
| トップ | 10 | 900ms |
| 当院について | 0 | 未検出 |
| はじめての方へ | 2 | 未取得 |

- 最大差は **10件**、`motion_consistent: false`。
- 「当院について」のdominantKindが`fade`でも、検出数0件なのでfade演出が存在すると判断しない。
- 各ページのkeyframes検出は1件。本文リビール数とは別の指標。
- ページ遷移ライブラリ・ローダー候補は未検出。**専用の遷移演出は確認できない**。

**prefers-reduced-motion**

- 通常時10件、軽減設定時も10件。
- 実測判定は **非対応**。
- 借用実装では軽減設定時に本文を初期状態から表示し、移動・スクロール連動を停止する。参照の不備まで再現しない。

### 7. 借りるべき点 / 借りてはいけない点

**`must_borrow`**

1. **トップを概要と振り分け、下層を判断材料の詳細にする。** 医院理解・初診手順・診療分類を別の行き先に対応させる。
2. **共通シェルとページ固有本文を分ける。** ヘッダー、下層タイトル帯、予約帯、医院情報、最下段ナビを共有する。
3. **写真と文章の配置規則を共有し、本文構造は目的で変える。** 医院紹介は空間別、初診案内は手順別、診療入口はカード別にする。
4. **共通工程の後で対象者別に分岐する。** 最初から成人・小児に分けず、共通の相談・検査過程をまとめて重複を抑える。
5. **各下層末尾に関連ページ2件と予約導線を置く。** 続けて理解したい人と、行動したい人の両方を受け止める。

**`do_not_copy`**

- 医院名、ロゴ、人物・院内写真、本文コピー、特徴の固有表現。
- 鳥・曲線・番号・淡色欧文の具体的な組み合わせを、そのまま識別意匠として転用すること。
- **reduced-motion非対応**。
- トップ10件・医院紹介0件という、ページ間のリビール量の偏り。
- 下層の可視パンくず不足。
- 地図・カレンダーが未表示の場合に残る大面積の空白。代替リンクや読み込み失敗時の縮退表示を設ける。
- 初診ページ末尾のアクセス情報と共通フッターの重複。詳細案内と共通要約に役割を分ける。
- トップと医院紹介で特徴3点をほぼそのまま反復する構成。下層側には根拠や具体例を追加する。
- 英語見出しが日本語より強くなる階層。日本語の情報探索を優先して調整する。

### 8. この業種の website に共通しそうな型

今回の1件から抽出できるのは、主に**地域の矯正歯科サイトの設計仮説**である。病院全般の共通仕様とは断定しない。

| 型 | この参照での根拠 | 一般化の範囲 |
|---|---|---|
| 医院理解・初診案内・診療別案内を分ける | 異なる疑問に対して独立した導線がある | 地域クリニック向けの基本案として利用可能 |
| 担当者と施設を別々の証拠として提示する | 院長経歴、空間用途、設備説明がある | 他診療科での重み付けは要追加観測 |
| 治療前から治療後までの流れを示す | 検査・計画・治療・保定まで段階化している | 長期通院型の診療で適用候補 |
| 年齢や対象者で情報を分岐する | 成人と小児で工程・ページを分ける | 矯正歯科では適用候補。他業種は要追加観測 |
| 予約と来院情報を全ページに置く | 電話・Web予約・診療時間・地図を共有する | 配置頻度とフッター長は要追加観測 |
| 実際の人・場所の写真で来院前の確認を助ける | 受付、診療室、スタッフ、外観を多数掲載する | 写真点数や非対称配置まで業種共通とはいえない |

料金、リスク説明、FAQ、症例の置き場所と深さは、今回の3キャプチャだけでは評価できない。サイト全体の不足とは断定せず、診療別ページと同業他サイトで**要追加観測**とする。
hook: Stop
hook: Stop Completed

---

### run: corporate-20260909-0940（2026-09-09）

参照: iro Inc. （https://iro-inc.jp/） / 業種: 制作・開発・企画・マーケティング・コンサル

### 1. サイトマップ

解析対象は添付3ページと付属の計測JSON。寸法は元画像の幅1440pxを基準とした概算。非掲載ページの本文、リンク先での操作、モバイル表示は未確認。

- トップ `/`：ロゴと作品断片を表示し、実績・所属クリエイター・会社情報・問い合わせに振り分ける。
- `WORK (46)` `/work/`：46件の制作実績を画像と案件名で一覧化する。
- `MANAGEMENT (1)` `/management/`：所属クリエイター1名のプロフィール、依頼窓口、作品群を提示する。
- `ABOUT` `/about/`：会社情報への入口。本文構成は未確認。
- `CONTACT`：問い合わせ導線。独立ページ・メール起動・モーダルのいずれかは未確認。

グローバルナビは上記のトップを除く4項目。階層メニューは画像上にない。トップへの入口はロゴと下層の `HOME` 表記。フッターだけにある追加ページは確認できない。

WORKの案件別詳細、MANAGEMENTの人物別詳細については、一覧からの遷移先を確認できていないためサイトマップに補完しない。

### 2. ページ共通のシェル

**基本骨格**

画面幅を12列に分割。1440px幅では1セル約120×82px、セル間の黒い線は約2px、角丸は約6〜8px。ロゴ、写真、ナビ、空白を同じセル寸法に合わせる。中央に最大幅コンテナを置かず、左右端まで使う。

**ヘッダー**

- 独立した横長ヘッダー帯はない。左上にロゴ、左下にナビ、右上に下層の現在地表示を分散する。
- トップのロゴは約620×325px。下層では約320×164pxに縮小する。
- 下層は上端約164px＝2セル行をロゴ・背景グリッドに使い、その直下から本文を開始する。
- ボタン形状のCTAはなく、問い合わせはナビの `CONTACT`。
- 左下ナビは約360×164px。3ページとも初期900pxの画面下端に相当する位置に写っており、ビューポート固定を示唆する。ただし固定指定やスクロール時の挙動は未確認。
- モバイルのメニュー開閉方法は資料なし。ハンバーガー式とは断定できない。

**フッター**

通常のページ末尾に置く多段フッターは、3キャプチャで確認できない。代わりに左下の1パネルがナビと補助情報を兼ねる。

- 左側：ナビ4項目を縦積み。
- 右上：`LOCATION TIME` と時刻。
- 右下：`(COPYRIGHT 2025)`。
- トップ右下には別パネルで、事業を示す短文と所在地を約4行で表示。
- 会社概要表、代表者、法務リンクなどは、この共通領域には見えない。

**パンくず**

下層右上に `[ HOME ] --- WORK`、`[ HOME ] --- MANAGEMENT`。幅約240px・高さ約82pxの暗色パネルに小文字サイズで置く。トップにはない。

**下層ページヘッダ帯**

専用の大見出し帯・背景写真はない。共通グリッドの上部2行がページ導入領域となり、ページ名は右上のパンくずで示す。

### 3. トップページの構成

縦に複数セクションを積む形式ではなく、**1画面に複数の機能を配置する構成**。以下は画面内の上から下への配置順であり、高さは重複する。

| 領域 | 役割 | 主要素 | 高さの目安 |
|---|---|---|---|
| 左上ロゴ領域 | 企業識別 | 2段の幾何学的ロゴ。グリッド線がロゴの上を横断 | 約325px／36vh |
| 全面タイル領域 | 制作物の断片を見せる | 12列×11行程度。暗色セル、マゼンタのセル、写真・映像断片を混在 | 約900px／100vh |
| 左下ナビ領域 | 下層への振り分け | 4項目ナビ、件数、時刻、著作権表記 | 約164px／18vh |
| 下中央インジケーター | 表示状態を伝える | `01`・`02`、角括弧、横長のマゼンタ帯 | 下端約82px／9vh |
| 右下会社情報 | 事業と所在地の補足 | 英文の短い事業説明、住所 | 下端約82px／9vh |

長い見出し、本文、サービス説明、ニュース一覧は見えない。トップの役割を「作品の印象提示と入口」に絞り、説明を下層に分担している。

付属JSONでは画像7要素・動画4要素。画面上の写真断片数とは一致しないため、セル数を素材数として数えない。`01`・`02`の切り替え操作や自動送りは未確認。

トップの計測値 `viewportsTall: 0` は、900px高の画像が存在することと整合しない。ページが無高・無内容という意味には採用しない。

### 4. 下層ページの構成

**WORK：案件を比較・探索する一覧**

元画像は1440×4172px、約4.6画面分。

| 領域 | 役割 | 主要素 | 高さの目安 |
|---|---|---|---|
| 共通導入 | 企業識別と現在地 | 小型ロゴ、右上パンくず、背景グリッド | 約164px |
| 実績一覧 | 制作領域・成果物の提示 | 46カード。4列×11行＋最終2件。画像と案件名 | 約3840px |
| 末尾余白 | 一覧の終端 | 背景テクスチャ | 約170px |

- カード幅は約240px＝2セル。画像は約240×164px、案件名帯は約82px、合計約246px。
- 1列分の配置単位は約360px＝3セル。各行を左右に約120pxずらし、直線的な4列一覧に位置の変化を付ける。
- 行ピッチは約328px、カード間の縦空白は約82px。
- 画像は化粧品、自動車、飲料、番組、イベントなどの制作成果物。案件名は短く、説明文は付けない。
- フィルター、検索、ページネーションはキャプチャ内にない。
- 上部約1画面ではセル境界が見え、それ以降は同じ暗色テクスチャ上でカードが続く。理由や動作は静止画だけでは確定できない。

**トップとの作り込みの差：同等。** トップのタイル寸法をカードと配置間隔へ展開し、46件の内容を約4.6画面に載せている。説明量は少ないが、下層が空の構成ではない。

**MANAGEMENT：人物情報と作品集を並置**

元画像は1440×2702px、約3画面分。左4列＝約480px、右8列＝約960pxに分かれる。

| 領域 | 役割 | 主要素 | 高さの目安 |
|---|---|---|---|
| 共通導入 | 企業識別と現在地 | ロゴ、パンくず | 約164px |
| 左プロフィール | 制作者と依頼先の確認 | 氏名、`PHOTOGRAPHER`、4行程度の略歴、メール・電話・担当者情報 | 本文上部約490px |
| 右 `CLIENT WORK` | 商業写真の実績提示 | ラベル1列＋写真7列、約18行 | 約1475px |
| 右 `MOVIE` | 動画制作の実績提示 | ラベル1列＋映像サムネイル。約2行 | 約164px |
| 右 `ARTWORK` | 自主制作・表現領域の提示 | ラベル1列＋写真7列、約9行 | 約740px |
| 末尾余白 | ページ終端 | 黒背景 | 約160px |

プロフィールと作品一覧は上下順ではなく、上端をそろえた並列配置。作品側は1セル約120×82pxの写真を約2pxの間隔で詰める。付属JSONでは画像186要素・動画10要素。案件数とは区別する。

左カラムはプロフィールとナビの下に大きな黒い空白が残り、右の作品集だけが下方へ続く。末尾のフォームやCTA帯は見えない。

**トップとの作り込みの差：同等以上の情報量。** 共通グリッドを維持しつつ、人物情報と3種類の作品群に構成を変えている。WORKが案件単位、MANAGEMENTが人物に属する作品単位という役割分担がある。

### 5. ビジュアルシステム

**配色**

以下はJPEG画素からの採取値。CSS指定値ではなく、圧縮や半透明表示を含むキャプチャ上の実測値。

| 系統 | 実測値 | 用途 |
|---|---|---|
| メイン：共通の暗色面 | `#202020` | ナビ、案件名帯、作品一覧の下地 |
| アクセント：トップ | `#FF00FE` | 塗りセル、ロゴ、ナビ。`#FF00FF`の画素も多数 |
| アクセント：WORK | `#FF5700` | ロゴ内部の高彩度画素の最頻値。案件名・ナビも橙系 |
| アクセント：MANAGEMENT | `#D8FF1B` | ロゴ内部の高彩度画素の最頻値。ラベル・ナビも黄緑系 |
| 背景：テクスチャ | `#2C2C2C`〜`#2F2F2F` | 細かな粒子を持つグリッド面 |
| 背景：黒 | `#000000` | セル境界、MANAGEMENT左下の空白 |

ページごとにアクセント色を替える一方、背景・文字の役割・セル形状を共通化している。色替えは装飾の追加ではなく、ページ識別のルールとして使う。

**タイポ**

- 欧文：等幅・ドット状に見える表示用書体。日本語：ゴシック系。正確なフォント名は未確認。
- 通常の巨大な文章見出しはない。最大要素は文字組みではないロゴ図形で、トップでは全体約620×325px。
- 1440px幅基準で、ナビは約16px、案件名・補助ラベルは約10〜12px、人物情報は約12〜14px。
- 欧文のカテゴリ名と和文の氏名・略歴・案件名が同居する。
- 本文は数行の短い情報群。長文を読むための広い行間や段落設計は主題になっていない。

**写真**

- トップ：広告写真・映像の一部をセルで切り出す。色面に覆われ、全体像が読めないものもある。
- WORK：46画像。人物、製品、車、イベント、広告グラフィックを案件単位で表示。寄り・引きは各制作物に依存。
- MANAGEMENT：画像186要素・動画10要素。人物や製品の寄り、室内・建築、自然風景の引きを混在。
- 共通ルールは被写体や色調の統一より、**表示枠の寸法と配置の統一**にある。

**余白**

WORKは約82pxの縦間隔と左右の1セル分の空きを使う。MANAGEMENTは写真間約2pxまで詰める。用途によって密度を変え、寸法の基準だけを共有する。

### 6. モーション

**計測結果と実装判断を分ける必要がある。** `dominant_reveal: fade` は、全種別の検出数が0であるため、フェード演出の存在を裏付けない。

| 項目 | 観測結果 | 後工程への指示 |
|---|---|---|
| リビール | トップ0、WORK 0、MANAGEMENT 0 | 本文への一律フェード・せり上がりを参照由来として指定しない |
| duration | `null` | 実測値なし。300msなどを観測値として補完しない |
| easing | 空配列 | 未確認 |
| ライブラリ | 全3ページにGSAP 3.5.1、ScrollTrigger、Lenis | 読み込みは確認済み。個別要素への適用は未確認 |
| スクロールトリガー | 提示サマリーでは0 | pin・scrub・視差効果の採用根拠はない |
| ヘッダー | `null` | 固定、縮小、隠れる動作を断定しない |
| ページ遷移 | 専用ライブラリ検出なし、ローダー候補5要素 | 初回ローダーとページ間遷移を区別。遷移演出の有無は未確定 |

画面には下層の `[100%]`、ログには `loadingPercentage`、`loading`、`gridBox loader delete` がある。グリッドと進捗表示を使う読み込み処理は示唆されるが、開始条件・消去順序・所要時間は得られていない。

付属のMANAGEMENT生ログでは、`gridBox active` のopacityが1→0になる差分も記録されている。ただし追加classなし、CSS transitionは0秒。集計のリビール0件とは別の背景セル変化として扱い、クラス切り替え式のフェード実装と断定しない。

- **ページ間の演出量**：検出リビールは0／0／0、最大差0。検出範囲では一致するが、トップの動画やローダーを含む総演出量が同じとは限らない。
- **reduced motion**：判定は `respected: false`。通常・軽減とも検出リビール0件なので、どの動きが抑制されなかったかは特定不能。対応済みとは扱わない。
- **再設計時の指定**：本文・ナビを演出完了待ちにしない。動きを追加するなら、対象、発火条件、時間、停止条件を別途定義し、`prefers-reduced-motion: reduce` では装飾アニメーションや自動スクロールを停止する。これは参照実測値ではなく改善指示。

### 7. 借りるべき点 / 借りてはいけない点

**`must_borrow`**

1. **ページの役割を分ける**：トップは入口、WORKは案件探索、MANAGEMENTは人物の能力と依頼先の確認を担う。
2. **寸法の共通ルールを持つ**：ロゴ、ナビ、作品枠、余白を12列グリッドの整数倍で設計する。
3. **内容に応じて一覧密度を変える**：案件比較は画像＋名称＋空白、人物作品集は小画像を連続配置する。
4. **所属関係を構造で示す**：人物プロフィールと、その人物の商業写真・動画・自主制作を同じページで結び付ける。
5. **ナビの順番と件数表示をそろえる**：全ページで同じ4項目を保ち、実績数・所属人数を入口で伝える。

**`do_not_copy`**

- 企業名、ロゴ形状、案件名、人物名、写真・映像、略歴、住所、連絡先、コピーそのもの。
- 約10px級の小文字と暗い画像表示。案件名や作品内容を読む負担になる。
- トップで写真が色面に隠れ、事業内容を示す文章も小さい構成。初訪問者が提供サービスを判別しにくい。
- 46件・約4.6画面の一覧に、画像上では検索・絞り込みが見えない点。
- MANAGEMENT左下に長く残る空白。別案件では人物情報量と作品量に合わせて再配分する。
- reduced-motion対応を確認できない状態、ロード完了らしい `[100%]` 表示がキャプチャに残る状態。残留原因は未確認だが、そのまま完成仕様にはしない。

### 8. この業種の website に共通しそうな型

この1件から抽出できるのは、**広告制作・クリエイター所属事業を持つ企業サイトの仮説的な型**。

- 信頼の根拠を、長い事業説明より制作実績の画像に置く。
- 「会社が受けた案件」と「所属する人の作品」を異なる探索軸として用意する。
- トップで全情報を説明せず、実績・人材・会社情報・問い合わせへ振り分ける。
- 人物の作品を見る場所に依頼窓口を併記し、能力確認から連絡への移動を短くする。
- 共通シェルを保ちながら、下層では情報の種類に合わせて密度と構成を変える。

所属クリエイター事業の有無、案件詳細での担当範囲の説明、実績の絞り込み、問い合わせフォーム、モバイルナビは**要追加観測**。暗色背景、蛍光色、12列タイル、英語ナビはこの参照の表現であり、広告業界共通の型とは断定しない。
hook: Stop
hook: Stop Completed

---

### run: corporate-20260903-1448（2026-09-09）

参照: EZOBOLIC （https://corp.ezobolic.jp/） / 業種: other

### 1. サイトマップ

PC版グローバルナビは階層表示を持たず、次の9項目を5カラムに分けて配置している。[公式トップページ](https://corp.ezobolic.jp/)

- `Philosophy` — ブランドのStatement、Concept、Vision、Value、シンボル思想を説明する。
- `Company` — `/profile/`へ遷移。代表メッセージ、2017〜2026年の沿革、会社概要を掲載する。
- `Products` — プロテイン、食品ブランド、ライフスタイル商品、ジム事業を紹介する。[Products](https://corp.ezobolic.jp/products/)
- `Stockist` — 札幌・原宿の店舗情報、営業時間、住所、店舗一覧を掲載する。[Stockist](https://corp.ezobolic.jp/stockist/)
- `Laboratory` — 商品開発拠点と研究・製造姿勢を説明する。[Laboratory](https://corp.ezobolic.jp/laboratory/)
- `Information` — 年別・日付順のニュース一覧。
- `Contact` — 問い合わせの受け皿。
- `Recruit` — 採用メッセージ、募集職種、給与、福利厚生、選考フロー、応募リンクを掲載する。[Recruit](https://corp.ezobolic.jp/recruit/)
- `Store` — 外部ECサイトへ遷移する。

SP版の全画面メニューでは、同じページを次の階層に再編している。ラベルは実際の表記どおり。

- `Company`
  - `Philosophy ↗`
  - `Profile ↗`
- `Service`
  - `Products ↗`
  - `Stockist ↗`
  - `Lab ↗`
- `Information`
  - `News ↗`
  - `Contact ↗`
- `Recruit`
  - `Recruitment ↗`
- `Store`
  - `Online shopping ↗`

フッターにだけ出る外部リンクは次の3項目。

- `Instagram` — 公式SNS。
- `Youtube` — 公式動画チャンネル。
- `LINE` — LINEアカウント。

プライバシーポリシー、利用規約、サイトマップは確認できない。

### 2. ページ共通のシェル

- ヘッダー
  - 最上部に高さ28pxの固定マーキー帯があり、ブランド説明と札幌・東京の住所が横移動する。
  - PCヘッダーはその下、`top: 28px`で固定。実測高さ121px。スクロールしても高さ・位置・背景・classは変わらない。
  - ロゴは5分割グリッドの第1列左端、左右余白24px、表示幅48px。
  - 残りのナビは第2〜第5列に配置。独立した強調CTAはなく、`Contact`と`Store`も通常テキストリンク。
  - SPヘッダーはマーキー直下に高さ60pxで固定。左に幅40pxのロゴ、右に70×10pxの二本線ボタン。
  - SPメニューは白い100dvhオーバーレイ。`clip-path`を上端から下方向へ開き、500ms、`cubic-bezier(0.19, 1, 0.22, 1)`。開いている間は本文スクロールを固定する。

- フッター
  - 視覚上は2段構成。上段は中央配置の大きな年輪状シンボル、下段は上罫線付きのユーティリティバー。
  - 下段はPCで3列。左に著作権、中央に札幌所在地、右にSNS 3項目。
  - 通常フッターが露出するサイトマップ項目は0。SP全画面メニュー内には9リンクを再掲する。
  - 会社情報は英語表記の札幌住所1件と著作権のみ。法人名、電話、事業内容はフッターに出さない。

- パンくず
  - 下層2ページともなし。ページ名だけで現在地を示している。

- 下層ページのページヘッダ帯
  - 独立した帯はない。
  - `#F5F5F5`のドットグリッド背景へH1と最初の写真群を直接配置する。
  - 1440px幅では1グリッドセルが約278px。H1領域はおおむねこの1セル分だが、そのまま写真コラージュへ連続する。
  - H1は左右端に語を分ける。例：`Company`／`Philosophy`、`Company`／`Profile`。

### 3. トップページの構成

基準キャプチャは1440×9030px、約10 viewport。

| 順序 | セクション | 役割 / 主要素 / 高さ |
|---|---|---|
| 1 | ヒーロー | ブランドの第一印象。全画面写真、横幅いっぱいの白い`ezobolic.`ワードマーク。本文・ボタンなし。7枚の背景候補を切り替える。約1.2 viewport。 |
| 2 | `Company` | 企業理解への振り分け。左に見出し、中央に暗色と青い面で構成された縦長写真、右に`Philosophy ↗`と`Company ↗`。約1.5 viewport。 |
| 3 | `Service` | 商品・店舗・研究開発への振り分け。中央にプロダクト／布地の寄り写真、右に`Products ↗`、`Stockist ↗`、`Lab ↗`。約1.6 viewport。 |
| 4 | `Information` | 更新情報と問い合わせへの振り分け。水面の横長写真、`News ↗`、`Contact ↗`。約1.4 viewport。 |
| 5 | `Recruit` | 採用ページへの入口。製造・店舗空間の写真、`Recruitment ↗`。約1.4 viewport。 |
| 6 | `Store` | ECへの送客。店舗内装の縦長写真、`Online Shopping ↗`。約1.7 viewport。 |
| 7 | シンボル／フッター | 年輪状シンボルを単独で大きく表示し、企業情報バーへ接続。約1.0 viewport。 |

トップは説明文を持たず、「5つの情報領域へ移動させる索引」に徹している。各領域は左見出し・中央写真・右リンクの同一構造で、見出しと写真をstickyにしてスクロール中の対応関係を維持する。

### 4. 下層ページの構成

#### Philosophy

キャプチャは1446×13686px、15.2 viewport。[公式ページ](https://corp.ezobolic.jp/philosophy/)

| 順序 | セクション | 役割 / 主要素 / 高さ |
|---|---|---|
| 1 | `Company Philosophy`／`Statement` | ページ主題の提示。左右分割H1、`Statement`／`Train Your Life`、雪原・滝・雪景色・抽象面の写真4点。本文・ボタンなし。約4.5 viewport。 |
| 2 | `Concept` | ブランドコピーの説明。`人生をみがこう。`、長文13行、生活動作の寄り写真。続けてブランド定義文と走る人物の写真を左右反転配置。合計約4.0 viewport。 |
| 3 | `Vision` | 中期的な目標。見出し、`終わりなき成長に、挑む輪を広げていく`、4行の本文。写真なし。約1.4 viewport。 |
| 4 | `Our Value` | 行動規範の提示。樹皮／自然写真2点、価値観5項目を`01:`〜`05:`で番号化。約2.2 viewport。 |
| 5 | `Life Training Circulation` | シンボルの意味を説明。`Our Symbol`、循環・年輪についての本文、黒地の同心円動画。約1.7 viewport。 |
| 6 | `Related Content`／フッター | 店舗写真、`Philosophy ↗`、`Company ↗`、年輪シンボル、共通フッター。約1.4 viewport。 |

トップとの差は「下層のほうが明らかに作り込まれている」。ページ長はトップ比1.52倍、写真配置9点＋動画1点、実測リビール11件。思想をコピー、長文、数値リスト、動画へ分解しているため、下層が簡素という状態ではない。

#### Company Profile

キャプチャは1440×8953px、9.9 viewport。[公式ページ](https://corp.ezobolic.jp/profile/)

| 順序 | セクション | 役割 / 主要素 / 高さ |
|---|---|---|
| 1 | `Company Profile` | 企業像と拠点を写真で提示。左右分割H1、店舗外観の大型写真、什器・資料と商品棚の写真2点。約3.6 viewport。 |
| 2 | `Message` | 創業意図と「100年続くブランド」という目標を説明。左に見出し、右に1段落。約1.3 viewport。 |
| 3 | `History` | 2017〜2026年の沿革。年を中央列、出来事を右側に置く縦一覧。10年分を同一書式で掲載。約2.5 viewport。 |
| 4 | `Company Profile`表 | 会社名、所在地、代表者名、電話番号、事業内容の5項目。ラベルと値の2列。約1.3 viewport。 |
| 5 | `Related Content`／フッター | Philosophy写真とリンク、年輪シンボル、共通フッター。約1.2 viewport。 |

トップとほぼ同じページ長で、信頼情報もメッセージ・沿革・会社概要へ分解されている。Philosophyほど写真やモーションは多くないが、下層として明らかに簡素ではない。

### 5. ビジュアルシステム

- 配色
  - メイン：`#111111`。本文・見出し。
  - アクセント：`#000000`。6×6pxのグリッドドット、罫線、メニューアイコン。
  - 背景：`#F5F5F5`。全ページ、マーキー、フッター。
  - UIには色相を持つアクセントカラーがない。青、黄、緑などは写真内だけに限定する。

- タイポ
  - 欧文はTWK Lausanne系の`ls-350`／`ls-450`、和文は游ゴシック系。見出し・本文ともゴシック。
  - 下層H1/H2は最大75px、トップ領域見出しは60px、右側リンクは28px。ヒーローのワードマークはSVGをほぼ画面幅いっぱいに表示する。
  - 本文は下層で20px、行高1.75。本文幅は全幅にせず、5列中2列程度へ制限する。
  - `Company Philosophy`、`Our Value`、`人生をみがこう。`など和欧混植あり。英語は飾りではなく情報分類として使う。

- 写真
  - トップ：キャプチャ上6点。人物1、製品・素材1、水面1、製造／店舗空間2、抽象寄り写真1。ヒーローには未表示分を含む7枚の切替候補がある。
  - Philosophy：写真配置9点＋動画1点。雪、滝、樹皮、蒸気など自然の寄り／引きと、靴を結ぶ動作、走る人物、店舗空間を混在させる。
  - Profile：写真4点。店舗外観、店内什器、商品棚、関連ページ用の抽象写真。
  - 商品を白背景で並べるより、身体動作・自然表面・現場空間を組み合わせてブランド思想を補強している。

- 余白
  - 広い。1セクションが1.2〜4.5 viewportに達し、写真と文章の間に1〜3グリッドセル分の空白を置く。
  - 情報量の少ないトップも高さを詰めず、写真が切り替わるまでスクロール距離を確保する。

### 6. モーション

- 支配的なリビール
  - 集計上の支配種別は`fade`。設計された要素リビールはトップ0件、Philosophy 11件、Profile 3件で、下層では`fade-up`が支配的。
  - リビールのdurationは実測不能で`null`、検出easingは`ease`。参照値として任意のmsを補わないこと。
  - 明示的に測れるヒーロー画像切替は1200ms、`ease-in-out`。3秒ごとに次の背景へ切り替える。
  - 画像パララックスは固定durationを持たず、スクロール進捗へ`-10%`から`+10%`をscrub接続し、`ease: none`。

- 発火の仕組み
  - ヒーロー：`is-active` classのタイマー切替。
  - 写真：GSAP 3.12.2＋ScrollTrigger。`start: "top bottom"`、`end: "bottom top"`、`scrub: true`、pinなし。
  - 慣性スクロール：Lenis。duration 1200ms、指数減衰型easing、タッチではsmooth無効。
  - PhilosophyのStatement消失はGSAPでopacityをスクロール連動。写真マスクにはclip-pathのスクロール連動もある。
  - 最上部マーキーはGSAPで28秒の無限linear移動。

- ヘッダー
  - `position: fixed`。スクロール前後とも高さ121px、top 28px、透明背景。
  - 縮小、退避、背景色変更、class切替はいずれもなし。

- ページ遷移
  - Barba、Swup、View Transitions、ローダー候補は検出されず、ページ遷移演出なし。

- ページ間の演出量
  - トップ0件、Philosophy 11件、Profile 3件。最大差11件で`motion_consistent: false`。
  - 共通するのはLenis、マーキー、画像パララックス。コンテンツ演出量はページごとに揃っていない。

- `prefers-reduced-motion`
  - サイト全体として非対応。マーキーを停止するCSSだけはあるが、GSAPパララックス、Lenis、ヒーロー切替、動画を包括的に止めないため、実測判定は`respected: false`。

### 7. 借りるべき点 / 借りてはいけない点

`must_borrow`

- トップを長い説明ページにせず、`Company / Service / Information / Recruit / Store`の5領域へ振り分ける索引にする。
- 左に領域名、中央に代表写真、右に下位リンクという3点セットを全領域で反復する。
- 5列グリッドとドットをトップ・下層・フッターで共有し、ページが変わっても座標系を変えない。
- 下層を「大見出し＋写真」だけで終わらせず、PhilosophyはStatement／Concept／Vision／Value、ProfileはMessage／History／会社概要に分解する。
- 各下層末尾に`Related Content`を置き、隣接ページへ戻す。フッターだけに回遊を依存させない。

`do_not_copy`

- `EZOBOLIC`、`人生をみがこう。`、`Train Your Life`、製品名、会社沿革、写真、ロゴ、年輪シンボルそのもの。
- `Company`と`Profile`、`Laboratory`と`Lab`、`Information`と`News`、`Recruit`と`Recruitment`のように、表示場所で同一ページのラベルを変えること。
- 下層ページでパンくずを省略すること。
- トップ0件、Philosophy 11件、Profile 3件というモーション量の不均衡。
- `prefers-reduced-motion`をマーキーだけに限定し、パララックスや自動切替を残すこと。
- ヘッダーに明確な問い合わせCTAがなく、通常フッターにもサイトマップ・法務リンクがない構成。
- 28pxのマーキーへ長い住所を常時流し、重要なナビより高いz-indexで固定すること。

### 8. この業種の website に共通しそうな型

- スポーツ／ウェルネス系のD2C企業では、`ブランド思想 → 会社情報 → 商品 → 店舗 → 開発背景 → ニュース → 問い合わせ → 採用 → EC`という役割分担は流用可能。
- コーポレートサイトをECの代替にせず、信頼形成と事業理解を担わせ、購入は`Store`へ分離する型は一般化できる。
- Philosophyで情緒的な理由、Productsで具体的な商品、Stockistで購入場所、Profileで法人情報を説明すると、トップへ情報を詰め込まずに済む。
- 自社店舗や開発拠点を持つ企業では、`Stockist`と`Laboratory`を独立ページにする価値がある。店舗・研究設備を持たない企業への一般化は要追加観測。
- 自然写真、巨大な余白、ドットグリッド、年輪モチーフがスポーツ業種全体の型かは要追加観測。これは業種慣習より個別ブランド表現の比重が高い。
- 下層をトップ以上の長さと写真量で設計する傾向が同業他社にもあるかは、参照1件では断定できず要追加観測。
hook: Stop
hook: Stop Completed
