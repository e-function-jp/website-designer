---
interface Props { center?: boolean; class?: string }
const { center = false, class: className = '' } = Astro.props;
---
<footer class={`footer ${center ? 'footer-center' : ''} bg-base-200 text-base-content p-10 ${className}`}>
  <slot />
</footer>
