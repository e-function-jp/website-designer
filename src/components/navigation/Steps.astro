---
interface Props { vertical?: boolean; class?: string }
const { vertical = false, class: className = '' } = Astro.props;
---
<ul class={`steps ${vertical ? 'steps-vertical' : ''} ${className}`.trim()}>
  <slot />
</ul>
