---
interface Props { variant?: 'primary' | 'secondary' | 'accent' | 'info' | 'success' | 'warning' | 'error'; class?: string }
const { variant = 'success', class: className = '' } = Astro.props;
---
<span class={`status status-${variant} ${className}`}></span>
